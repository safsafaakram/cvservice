This legacy guide has been replaced by:

1. E2E_TEST_GUIDE.md
2. e2e_workflow_test.ps1

The current architecture is:

Auth Service -> JWT
Frontend -> Auth Service / Job Service / CV Service
CV Service -> RabbitMQ -> AI Worker -> RabbitMQ -> Notification Service
AI Worker -> Job Service (internal token)
AI Worker -> CV Service (internal token)

Use the new guide because the old instructions referenced the removed CV result consumer path.
